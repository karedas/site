---
import '@/components/section-title.css';

type Props = {
  num: string;
  title: string;
};

const { title } = Astro.props;
---
<h2 class="section-title" data-reveal>
  {title}<span class="rule" aria-hidden="true"></span>
</h2>
